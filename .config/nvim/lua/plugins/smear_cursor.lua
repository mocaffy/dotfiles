return {
  "sphamba/smear-cursor.nvim",
  opts = {
    cursor_color = "#ff00ff",
    -- stiffness = 0.3,
    -- trailing_stiffness = 0.1,
    -- trailing_exponent = 5,
    -- never_draw_over_target = true,
    -- hide_target_hack = true,
    -- gamma = 1,
  },
}
